package com.fititu.logoquizitu.View

import androidx.fragment.app.Fragment
import androidx.fragment.app.FragmentTransaction

interface ISelectModeView {
    fun changeView(fragment: Fragment)

//    fun changeViewWithParam(fragment: Fragment, ) no use for this in the main menu
}